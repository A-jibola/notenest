<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Notenest" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body data-theme="<%= current_user&.theme || 'default' %>" data-font="<%= current_user&.font || 'clean-sans' %>">
    
    <%= render 'layouts/sidebar' %>


    <main class="flex-1 ml-0 md:ml-64 p-6 overflow-y-auto mt-16 md:mt-0">
        <% if notice %>
          <p class="notice"><%= notice %></p>
        <% end %>
        <% if alert%>
          <p class="alert"><%= alert %></p>
        <% end %>

      <% if user_signed_in? %>
        <%= turbo_stream_from "notifications_user_#{current_user.id}"%>
      <% end %>

      <div id="notifications"  > 
        <% if user_signed_in? %>
          <div class="relative ml-4 pb-2">
            <button data-controller="notification" data-notification-target="bell" data-action="notification#belltoggle" id="notification-bell" class="absolute right-0 text-[var(--text-color)] hover:text-[var(--dark-color)]">
              <i class="fas fa-bell text-xl"></i>
              <span id="notification-badge" class="absolute -top-1 -right-1 bg-red-500 text-white text-xs rounded-full w-4 h-4 flex items-center justify-center hidden">!</span>
            </button>

            <!-- Dropdown -->
            <div id="notification-dropdown" class="hidden absolute right-0 mt-2 w-80 bg-white shadow-lg rounded-lg border border-gray-200 z-30 p-4">
              <h4 class="text-md font-semibold mb-2 text-[var(--text-color)]">Recent Notifications</h4>
              <div id="notification-list" class="space-y-2 max-h-60 overflow-y-auto">
                <%= render 'notifications/notifications' if user_signed_in? %>
              </div>
            </div>

          </div>
        <% end %>
      </div>


     <%= yield %>
    </main>

    <!-- Welcome Modal -->
    <div data-controller="welcome-modal" class="hidden fixed inset-0 bg-black bg-opacity-50 z-50 flex items-center justify-center" data-welcome-modal-target="backdrop">
      <div data-welcome-modal-target="modal" class="hidden bg-white rounded-lg shadow-xl max-w-md w-full mx-4 p-6 z-50">
        <h2 class="text-2xl font-bold mb-4 text-[var(--text-color)]">Welcome to Notenest</h2>
        <p class="text-[var(--text-color)] mb-6">
          The version you are accessing is a project mode which will deactivate user accounts after 1 - 2 days of user creation unless given privileged access. Thank you for trying it out.
        </p>
        <button 
          data-action="click->welcome-modal#dismiss" 
          class="w-full bg-[var(--primary-color)] text-white py-2 px-4 rounded hover:bg-[var(--dark-color)] transition"
        >
          I understand
        </button>
      </div>
    </div>

  </body>
</html>
