<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Notenest Email</title>
    <style>
      :root {
        --primary-bg: #DDF6D2;
        --accent-bg: #B0DB9C;
        --text-color: #333333;
        --button-color: #ffffff;
      }

      body {
        margin: 0;
        padding: 0;
        background: var(--primary-bg);
        font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
        color: var(--text-color);
        line-height: 1.6;
      }

      .email-container {
        max-width: 600px;
        margin: 40px auto;
        background: #ffffff;
        border-radius: 12px;
        overflow: hidden;
        box-shadow: 0 5px 20px rgba(0,0,0,0.05);
      }

      .email-header {
        background: var(--accent-bg);
        padding: 30px;
        text-align: center;
        font-size: 26px;
        font-weight: bold;
        color: #ffffff;
      }

      .email-body {
        padding: 30px;
        font-size: 16px;
      }

      .email-button {
        display: inline-block;
        margin-top: 20px;
        background: var(--accent-bg);
        color: var(--button-color);
        padding: 14px 28px;
        border-radius: 6px;
        text-decoration: none;
        font-weight: bold;
      }

      .email-footer {
        text-align: center;
        font-size: 12px;
        color: #999;
        padding: 20px;
      }

      a {
        color: var(--accent-bg);
        text-decoration: none;
      }
    </style>
  </head>
    <body>
        <div class="email-container">
        <div class="email-header">
            Notenest
        </div>
        <div class="email-body">
            <h2 style="margin-top: 0;">Welcome to Notenest,  <%= @email %>!</h2>
            <p>
            Thanks for signing up. Please confirm your account by clicking the button below.
            </p>
            <p><%= link_to 'Confirm my account', confirmation_url(@resource, confirmation_token: @token), class:"email-button" %></p>
        <p style="margin-top: 30px; font-size: 14px; color: #666;">
          Didn’t create this account? You can safely ignore this email.
        </p>
      </div>
      <div class="email-footer">
        &copy; <%= Time.current.year %> Notenest — All rights reserved.
      </div>
    </div>
  </body>
</html>
